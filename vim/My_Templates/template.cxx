/*
ID: balor1
LANG: C++14
TASK: 
*/

#include <bits/stdc++.h>
using namespace std;

#define MOD 1000000009

#define endl '\n'
#define FOR(x,n,inc) for(int i=x; i < n; i+=inc)
#define print_vector(v) for(int i=0; i < v.size(); i++) { cout << v[i]; if(i != v.size()-1) cout << " "; }

#define interactive(choice) ios_base::sync_with_stdio(false); if (!choice) {cin.tie(0); cout.tie(0);}

#ifdef DEBUG
    #define debug true
#else 
    #define debug false
#endif

int main()
{
    interactive(false);
    freopen("test.in", "rt", stdin);
    freopen("test.out", "wt", stdout);

    return 0;
}
